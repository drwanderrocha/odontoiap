fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
edge-tts>=6.1.0
# whisper  # Descomente para STT no servidor (precisa de ~1.5GB RAM)
# openai-whisper  # Pacote alternativo
# chromadb>=0.5.0  # Descomente para RAG
